// function plus(p1, p2) {
//     return p1 + p2;
// }

const plus = (p1, p2) => {
    return p1 + p2;
}

//#work 1 : 빼기, 나누기, 곱하기 기능을 선언 해볼것 
const minus = (p1, p2) => {
    return p1 - p2;
}

const mul = (p1, p2) => {
    return p1 * p2;
}

const div = (p1, p2) => {
    return p1 / p2;
}

console.log(plus(2, 5));
console.log(minus(2, 5));
console.log(mul(2, 5));
console.log(div(2, 5));
